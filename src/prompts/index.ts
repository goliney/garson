import { input } from './input';
import { choices } from './choices';
import { multiChoices } from './multi-choices';
import { fuzzyPath } from './fuzzy-path';

export const prompts = {
  input,
  choices,
  multiChoices,
  fuzzyPath,
};
